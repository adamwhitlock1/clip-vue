const state = {
  clips: [],
};

const getters = {};
const mutations = {
  setStreamClips(state, clips) {
    state.clips = clips;
  },
};
const actions = {};

export default {
  state,
  getters,
  mutations,
  actions,
};
